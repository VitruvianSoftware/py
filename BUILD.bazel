"""Targets in the repository root"""

exports_files(
    [
    ],
    visibility = ["//:__subpackages__"],
)

# The gazelle binary + `gazelle` runner target live in //tools/gazelle (the
# default target for `aspect gazelle`). Run `aspect gazelle` to generate BUILDs.

# Hand-authored Pulumi macro + a standalone Pulumi Go module (its own go.mod):
# gazelle must not generate or rewrite BUILD files in either.
# gazelle:exclude infrastructure
# gazelle:exclude tools/pulumi

# uv reads pyproject.toml; tools/pytest also attaches it as test data.
exports_files(
    ["pyproject.toml"],
    visibility = ["//:__subpackages__"],
)
